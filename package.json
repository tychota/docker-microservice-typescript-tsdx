{
  "name": "test-ms-monorepo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "lerna": "^3.16.4"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap && lerna run patch",
    "bootstrap-prod": "lerna bootstrap -- --production --frozen-lockfile",
    "clean": "lerna clean",
    "start": "lerna run start --parallel",
    "build": "lerna run build",
    "update": "lernaupdate"
  },
  "devDependencies": {
    "lerna-update-wizard": "^0.16.0"
  }
}
